# ===============================
# Documind /backend/.env.example
# ===============================
# Bu dosya bir şablondur. Bunu kopyalayıp .env olarak adlandırın:
#   cd backend
#   cp .env.example .env
# Sonra .env içindeki placeholderları kendi değerlerinizle doldurun.

# --- OpenAI / Özetleme ---
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.2
SUMMARY_LANG=tr

# Size bir proxy verildiyse (örn. öğrenci backend'i):
SUMMARY_PROXY_URL=https://<backend-host-url>/api/summarize

# --- MCP (Gmail WebSocket Sunucusu) ---
MCP_WS=ws://localhost:8080

# --- Mail adresiniz ---
# Buraya kendi e-posta adresinizi yazınız. Newsletter özetleri bu adrese gönderilecektir.
USER_EMAIL=example@example.com

# --- Gmail OAuth ---
# NOT: Proje desktop (installed) flow kullanır. Aşağıdaki client_secret dosyasını
# Google Cloud Console'dan indirip bu path'e koymalısınız.
# Aşağıdaki iki değişkenin ikisi birden bazı modüllerde kullanılıyor; aynı path'i verin.
GOOGLE_CLIENT_SECRET_FILE=./backend/config/credentials/client_secret_XXXX.json
GOOGLE_CLIENT_SECRETS_FILE=./backend/config/credentials/client_secret_XXXX.json

# Token dosyası ilk yetkilendirmeden sonra otomatik oluşur:
GOOGLE_TOKEN_FILE=./backend/data/token.json

# Gmail izinleri (okuma/değiştirme + gönderme)
GMAIL_SCOPES="https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.send"

# --- Backend / API Ayarları ---
# UI ve agent, backend API'ye buradan erişir
API_BASE_URL=http://127.0.0.1:8000
NEWSLY_API=http://127.0.0.1:8000

# DailyDigestAgent için varsayılan saat dilimi
DEFAULT_TZ=Europe/Istanbul

# (Opsiyonel) Feature flag / davranış kontrolü
FORCE_ALL=0

# --- Veritabanları ---
# Uygulama kullanıcıları için (örnek)
NEWSLY_DB_URL=sqlite:///./backend/data/users.db
# DailyDigestAgent abonelikleri için tablo backend/data/newsly.db içinde otomatik oluşturulur.
